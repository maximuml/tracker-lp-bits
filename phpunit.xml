<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd" bootstrap="vendor/autoload.php" colors="true">
  <testsuites>
    <testsuite name="Unit">
      <directory suffix="Test.php">./tests/Unit</directory>
    </testsuite>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
  </testsuites>
  <php>
    <server name="APP_ENV" value="testing"/>
    <server name="APP_KEY" value="base64:WUbN2wa2kl3E1VDW4iKaH3RBHw3hKY7BK0hWEkBZmGg="/>
    <server name="BCRYPT_ROUNDS" value="4"/>
    <server name="CACHE_DRIVER" value="array"/>
    <server name="CHANNEL_NAME_SETTING" value=""/>
    <server name="CHANNEL_NAME_MODEL_EVENT" value=""/>
    <!-- DB_DATABASE: tests must never run against the dev/production
         database. The default below is a safe, isolated database; CI
         overrides it per-suite (nexusphp_unit_testing / nexusphp_feature_testing)
         and the E2E stack uses nexusphp_e2e_testing. The
         DestructiveEnvironmentGuard in TestCase::setUp() refuses to run
         if the name does not contain a test marker. -->
    <server name="DB_DATABASE" value="nexusphp_testing"/>
    <!-- Redis isolation: use DB index 15 and a test prefix to avoid
         clobbering dev data (dev uses DB 0 with no prefix).
         REDIS_HOST/REDIS_PASSWORD are inherited from .env. -->
    <server name="REDIS_DB" value="15"/>
    <server name="REDIS_PREFIX" value="nexusphp_test_"/>
    <!-- MeiliSearch isolation: disable Scout to avoid touching real indices. -->
    <server name="SCOUT_DRIVER" value="null"/>
    <server name="MAIL_MAILER" value="array"/>
    <env name="QUEUE_CONNECTION" value="sync"/>
    <server name="SESSION_DRIVER" value="array"/>
    <server name="TELESCOPE_ENABLED" value="false"/>
    <ini name="memory_limit" value="512M"/>
    <ini name="session.save_handler" value="files"/>
    <ini name="session.save_path" value="/tmp"/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./app</directory>
    </include>
  </source>
</phpunit>
